<%= form_with(model: spy) do |form| %>
  <% if spy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(spy.errors.count, "error") %> prohibited this spy from being saved:</h2>

      <ul>
        <% spy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :mission, style: "display: block" %>
    <%= form.text_field :mission %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
